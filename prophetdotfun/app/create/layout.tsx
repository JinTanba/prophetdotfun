export const metadata = {
    title: "予言を作成 - Prophet",
    description: "新しい予言を作成し、賭けを開始します。",
};

export default function CreateLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
} 