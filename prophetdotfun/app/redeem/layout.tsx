export const metadata = {
  title: '予言をRedeem - Prophet',
  description: '予言の結果を確認し、報酬を受け取ります。',
};

export default function RedeemLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 